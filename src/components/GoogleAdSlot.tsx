import React from "react";

export interface GoogleAdSlotProps {
  slotId?: string;
  adFormat?: string;
  className?: string;
  label?: string;
}

/**
 * Cleaned ad slot component - completely disabled.
 * meridian Research is 100% ad-free.
 */
export function GoogleAdSlot(_props: GoogleAdSlotProps): React.ReactElement | null {
  return null;
}
