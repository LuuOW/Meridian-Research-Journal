rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global safety net - deny all by default
    match /{document=**} {
      allow read, write: if false;
    }

    // Allow read and write for the blogs collection
    match /blogs/{blogId} {
      allow read, write: if true;
    }
  }
}
